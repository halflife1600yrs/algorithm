#include <bits/stdc++.h>

using namespace std;

int main()
{
    int cas, n, m;
    int step[10005];
    scanf("%d", &cas);
    while(cas--)
    {
        scanf("%d %d", &n, &m);
        for (int i = 0; i < n;i++)
        {
            
        }
    }
}