#include <bits/stdc++.h>

using namespace std;

int arry[100005];
int smaller[100005];

int main()
{
    int n, m;
    while(~scanf("%d %d", &n, &m))
    {
        for(int i = 1; i <= n; i++) scanf("%d", &arry[i]);
        for(int i = 1; i <= n; i++)
        {
            
        }
        for(int i = 0; i < m; i++) s
    }
}