#include <bits/stdc++.h>

using namespace std;
const int mode = 1e9 + 7;
const int sz = 1e6 + 5;
typedef long long ll;
char str[sz];
int next[sz]; 
int main()
{
    int n;
    scanf("%d", &n);
    while(n--)
    {
        scanf("%s", &str);

    }
}